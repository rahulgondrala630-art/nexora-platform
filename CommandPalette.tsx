import { useEffect, useState } from 'react';
type Props = { open: boolean; onClose: () => void };
export default function CommandPalette({ open, onClose }: Props) { const [query, setQuery] = useState(''); useEffect(() => { const handler=(e:KeyboardEvent)=>{if(e.key==='Escape')onClose();}; window.addEventListener('keydown',handler); return()=>window.removeEventListener('keydown',handler); },[onClose]); if(!open)return null; const actions=['Create a workstream','Review at-risk items','Open usage insights','Invite a collaborator']; return <div className="modal-backdrop" onClick={onClose}><div className="command-palette" onClick={e=>e.stopPropagation()}><div className="command-search"><span>⌕</span><input autoFocus value={query} onChange={e=>setQuery(e.target.value)} placeholder="Search commands…" /><kbd>ESC</kbd></div><div className="command-results"><small>QUICK ACTIONS</small>{actions.filter(a=>a.toLowerCase().includes(query.toLowerCase())).map((action,i)=><button key={action} onClick={onClose}><span>{['＋','◉','◫','♧'][i]}</span>{action}<kbd>↵</kbd></button>)}</div></div></div>; }
